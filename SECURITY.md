# Security

Do not run upstream installers or hooks blindly.

The default installer only clones repositories and copies skill directories. The `-InstallGsd` flag is the explicit exception and runs the local cloned Get Shit Done installer.

Report security concerns privately through the GitHub repository owner.
